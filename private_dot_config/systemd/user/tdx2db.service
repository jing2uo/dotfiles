[Unit]
Description=TDX2DB Data Sync
After=network-online.target

[Service]
Type=oneshot
ExecStart=mise x -- tdx2db cron --dburi 'clickhouse://localhost' --minline 5

[Install]
WantedBy=default.target
